# Plotting / analysis (versions match the figures shipped in this repo)
numpy>=2.4
pandas==3.0.3
matplotlib==3.11.0
seaborn==0.13.2

# Inference example (load the 4-bit base + LoRA adapter)
# Install a torch build appropriate for your platform/CUDA before these.
transformers>=4.44
peft>=0.15.1
accelerate>=0.33
bitsandbytes>=0.43  # 4-bit NF4 quantization; requires CUDA
